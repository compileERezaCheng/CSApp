name: Notify Release on Discord

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Custom Embed to Discord
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          RELEASE_TITLE: ${{ github.event.release.name || github.event.release.tag_name || 'Manual Test' }}
          RELEASE_TAG: ${{ github.event.release.tag_name || 'v0.0.0-test' }}
          RELEASE_URL: ${{ github.event.release.html_url || github.server_url && format('{0}/{1}', github.server_url, github.repository) }}
          RELEASE_BODY: ${{ github.event.release.body || 'This is a manual test dispatch to verify that the Discord Webhook is working!' }}
          REPO_NAME: ${{ github.repository }}
        run: |
          # Build JSON payload with custom embed
          PAYLOAD=$(jq -n \
            --arg content "🚀 **New update available!** <@&1550939281344823458>" \
            --arg username "GitHub Release Bot" \
            --arg avatar_url "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png" \
            --arg title "$RELEASE_TITLE ($RELEASE_TAG)" \
            --arg tag "$RELEASE_TAG" \
            --arg url "$RELEASE_URL" \
            --arg repo "$REPO_NAME" \
            --arg body "$RELEASE_BODY" \
            '{
              content: $content,
              username: $username,
              avatar_url: $avatar_url,
              embeds: [
                {
                  title: $title,
                  url: $url,
                  description: (if ($body | length > 1500) then ($body[0:1500] + "...\n\n*(truncated due to length limit)*") else $body end),
                  color: 3066993,
                  fields: [
                    {
                      name: "📦 Repository",
                      value: $repo,
                      inline: true
                    },
                    {
                      name: "🏷️ Tag",
                      value: $tag,
                      inline: true
                    }
                  ],
                  footer: {
                    text: "Released on GitHub",
                    icon_url: "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png"
                  },
                  timestamp: (now | todate)
                }
              ]
            }')

          # Envia a mensagem para o Discord
          curl -H "Content-Type: application/json" -X POST -d "$PAYLOAD" "$DISCORD_WEBHOOK"
